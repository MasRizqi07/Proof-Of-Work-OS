# Copilot Instructions

- This repository is a monorepo for a full-stack Node.js + React application named Proof of Work OS.
- The backend is located in `server/` and the frontend in `client/`.
- Use `npm install` from the root to install dependencies for both workspaces.
- Use `npm run dev` from the root to start both backend and frontend development servers.
